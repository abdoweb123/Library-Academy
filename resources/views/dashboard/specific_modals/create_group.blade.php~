<!-- Create Modal -->
<div class="modal fade" id="createGroup" tabindex="-1" aria-labelledby="createGroupModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="createGroupModalLabel">{{ trns('add_group') }}</h5>
                <button type="button" class="close px-3 btn btn-light" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">

                <form id="createForm" method="post" action="{{ route('dashboard.groups.store') }}" enctype="multipart/form-data">
                    @csrf
                    <div class="row justify-content-center">

                        <div class="row col-md-12 col-sm-12 mt-2">
                            <label class="p-0">{{ trns('title') }}</label>
                            <input type="text" class="form-control" name="title">

                            <label class="p-0">{{ trns('group') }}</label>
                            <select class="form-control" name="group_id">
                                <option value="" selected>----</option>
                            </select>
                        </div>
                    </div>

                    <button type='button' onclick="createModel(this)" class="btn btn-primary mt-3">{{ trns('save') }}</button>
                </form>
            </div>
        </div>
    </div>
</div>



